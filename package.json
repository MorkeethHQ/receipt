{
  "name": "receipt-monorepo",
  "private": true,
  "scripts": {
    "build:sdk": "cd packages/receipt-sdk && npm run build",
    "build:cli": "cd packages/receipt-cli && npm run build",
    "build:plugin": "cd packages/openclaw-plugin-receipt && npm run build",
    "build": "npm run build:sdk && npm run build:cli",
    "test": "cd packages/receipt-sdk && npm test",
    "dev": "cd demo/app && npm run dev"
  },
  "devDependencies": {
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "solc": "^0.8.34"
  },
  "dependencies": {
    "ethers": "^6.16.0"
  }
}

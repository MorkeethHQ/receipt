{
  "buildCommand": "cd packages/receipt-sdk && npm install && npm run build && cd ../../demo/app && npm install && npm run build",
  "outputDirectory": "demo/app/.next",
  "framework": "nextjs",
  "installCommand": "echo skip"
}

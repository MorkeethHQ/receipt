{
  "installCommand": "npm install && cd packages/receipt-sdk && npm install && npm run build && cd ../../demo/app && npm install",
  "buildCommand": "cd demo/app && npm run build",
  "outputDirectory": "demo/app/.next",
  "framework": "nextjs"
}
